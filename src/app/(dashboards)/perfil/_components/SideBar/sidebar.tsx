import Image from "next/image";
import React from "react";

const SideBar = () => {
  return (
    <aside className="sticky w-60 h-full min-h-screen bg-zinc-900">
      {/* <Image src={} /> */}
    </aside>
  );
};

export default SideBar;
